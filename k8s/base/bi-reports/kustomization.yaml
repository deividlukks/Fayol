apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - hpa.yaml

commonLabels:
  app.kubernetes.io/name: fayol-bi-reports
  app.kubernetes.io/component: bi
  app.kubernetes.io/part-of: fayol
