# Production secrets file (do not commit to git, use .gitignore)
# Use external secret management in production (AWS Secrets Manager, HashiCorp Vault, etc.)
DATABASE_URL=postgresql://prod_user:prod_pass@postgres-prod.db.amazonaws.com/fayol

# Prisma 7 - Connection Pool Settings (gerenciado pelo driver pg)
DATABASE_POOL_MAX=20
DATABASE_IDLE_TIMEOUT=10000
DATABASE_CONNECTION_TIMEOUT=0

REDIS_URL=redis://:prod_password@redis-prod.cache.amazonaws.com:6379
JWT_SECRET=your-production-jwt-secret
TELEGRAM_BOT_TOKEN=your-production-telegram-token
