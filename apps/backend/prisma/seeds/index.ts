/**
 * Exportação centralizada de todas as funções de seed
 * 
 * Permite importar seeds de forma organizada:
 * import { seedCategories, seedAdmin } from './seeds';
 */

export { seedCategories } from './categories.seed';
export { seedAdmin } from './admin.seed';
