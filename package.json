{
  "name": "fayol",
  "version": "0.1.0",
  "description": "Sistema Multiplataforma de Gestão Financeira Pessoal com IA",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md,json}\"",
    "health-check": "node scripts/health-check.js",
    "cleanup": "node scripts/cleanup.js",
    "cleanup:all": "node scripts/cleanup.js --all",
    "cleanup:force": "node scripts/cleanup.js --all --force",
    "predev": "npm run health-check"
  },
  "keywords": [
    "finance",
    "ai",
    "personal-finance",
    "nestjs",
    "nextjs"
  ],
  "author": "Fayol Team",
  "license": "MIT",
  "packageManager": "pnpm@10.16.1",
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^6.21.0",
    "@typescript-eslint/parser": "^6.21.0",
    "eslint": "^8.56.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-prettier": "^5.1.3",
    "prettier": "^3.2.5",
    "turbo": "^1.12.4",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=8.0.0"
  },
  "dependencies": {
    "@nestjs/platform-fastify": "^11.1.6"
  },
  "pnpm": {
  "overrides": {
    "fastify": "^4.26.2"
  }
}
}